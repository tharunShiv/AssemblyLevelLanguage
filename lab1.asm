.model small

.data
  A db 50d, 40d, 30d, 20d, 10d
  len dw $-A
  m1 db 'Key is found$'
  m2 db 'Key not found$'
  key db 10d

.code
  mov ax, @data
  mov ds, ax

  mov bx, len
  dec bx

n2: mov cx,bx
    lea si,A

n1: mov al,[si]
       inc si
       cmp al,[si]
       JBE noswap
       Xchg al,[si]
       mov [si-1],al
noswap:loop n1
dec bx
JNZ n2
lea si,A
mov di,si
add di, len
dec di
l1:cmp si,di
JA nf
mov bx,si
add bx,di
shr bx,01h
mov al,key
cmp al,[bx]
JZ found
JA nlow
mov di,bx
dec di
JMP l1

nlow: mov si,bx
inc si
JMP l1
found:lea dx,m1
JMP final
nf:lea dx,m2
final:mov ah,09h
int 21h
mov ah,4ch
int 21h
end


  















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































